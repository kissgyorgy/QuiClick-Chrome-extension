// Background script for QuiClick extension
// Minimal service worker — the popup and newtab page handle all logic.
// This exists to satisfy the manifest service_worker requirement.

console.log('QuiClick background service worker loaded');
